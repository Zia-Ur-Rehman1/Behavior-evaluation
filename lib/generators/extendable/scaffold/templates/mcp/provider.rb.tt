# frozen_string_literal: true

# LLM Provider — generated by extendable-rails.
#
# This is a thin subclass of the gem's Provider class. Override methods
# here if you need app-specific provider logic (custom headers, request
# transformations, etc.).
#
module Mcp
  module Llm
    class Provider < ExtendableRails::Mcp::Llm::Provider
      # Add app-specific customizations here.
    end
  end
end
